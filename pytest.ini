[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    e2e: browser end-to-end tests (require RUN_E2E=1 and playwright installed)
filterwarnings =
    ignore::DeprecationWarning
